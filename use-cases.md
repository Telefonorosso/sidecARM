* Use Git directly on source trees stored on Amiga volumes for `status`, `diff`, `log`, commits and repository maintenance.

* Edit live AmigaOS configuration files directly from Linux using modern tools like `nano`.

* Search the entire Amiga filesystem quickly with `grep`, `find`, `ripgrep`, and other standard Unix tools.

* Compare configuration files, backups, and directory trees with `diff`, and verify file integrity with checksums.

* Download files directly into Amiga storage using `wget` or `curl`, including downloads that require modern HTTPS/TLS support.

* Cross-compile Amiga software with `m68k-amigaos-gcc` on ARM64 Linux while keeping the source tree and generated binaries directly on Amiga storage.

* Run a complete edit-build-test development loop on the same machine: edit on AmigaOS or Linux, compile on ARM64, then immediately run the resulting 68k binary under AmigaOS.

* Run modern development tools such as `clang-format`, `cppcheck`, Python scripts and static-analysis utilities against Amiga source trees.

* Generate release archives, documentation and build artifacts using the much faster ARM64 CPU.

* Convert modern images such as WebP, AVIF, HEIC and SVG into formats usable by Amiga applications.

* Convert audio and video with `ffmpeg` and write the results directly to Amiga storage.

* Render PDFs and convert documents on Linux into formats that can be viewed or processed by Amiga software.

* Back up Amiga volumes incrementally with `rsync` to another machine, NAS or remote SSH server.

* Copy files directly between modern computers and Amiga storage using SCP or SFTP, without requiring equivalent modern protocol support in AmigaOS itself.

* Maintain a fast index of the Amiga filesystem for near-instant filename and full-text searches.
